# 呦呦英语 V2 — 游戏系统设计文档

> 版本：v1.3 | 日期：2026-05-28 | 状态：已通过二次审查，P0/P1 意见已全部整合
> 设计者：GameDesigner Agent | 基于项目代码审查 + PRD 分析 + 二次评审意见
> 审查状态：✅ v1.0→v1.1 审查修正 → v1.2 MVP 精选 → v1.3 二次评审意见整合

---

## 变更日志

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.3 | 2026-05-28 | 二次评审意见整合：Catch Stars 冷却、解锁门槛调整、Combo 视觉引导、时间上限、主题分组、离线模式 |
| v1.2 | 2026-05-28 | 精选 3 款 MVP 新游戏，调整实施路线为 3 阶段聚焦方案 |
| v1.1 | 2026-05-28 | 审查修正：4 个 P0 缺陷修复 + 6 个 P1 建议项调整 |
| v1.0 | 2026-05-28 | 初始版本 |

---

## 目录

1. [现有游戏系统现状](#一现有游戏系统现状)
2. [核心问题诊断](#二核心问题诊断)
3. [现有游戏优化建议](#三现有游戏优化建议)
4. [精选 3 款 MVP 新游戏](#四精选-3-款-mvp-新游戏)
5. [后续迭代游戏（本期不实施）](#五后续迭代游戏本期不实施)
6. [游戏系统架构优化](#六游戏系统架构优化)
7. [实施路线图（聚焦 3 阶段）](#七实施路线图聚焦-3-阶段)

---

## 一、现有游戏系统现状

### 1.1 三款已实现游戏概览

| 游戏 | 组件名 | 核心玩法 | 难度选项数 | 每轮奖励 | 代码行数 |
|------|--------|---------|-----------|---------|---------|
| **MatchGame** (找一找) | `MatchGame.vue` | 听音 → 选 emoji | 3/4/6 | ⭐ +1 | ~687行 |
| **ListenGame** (听音选词) | `ListenGame.vue` | 听音 → 选 emoji+文字 | 4/6/8 | ⭐ +1 | ~618行 |
| **MemoryGame** (翻翻乐) | `MemoryGame.vue` | 翻牌配对 emoji↔单词 | 2/3/4对 | ⭐ 按翻牌数定级 | ~466行 |

### 1.2 通用架构特征

- **难度系统**：Simple / Medium / Hard 三档，通过 `store.gameDifficulty` 控制
- **进度条**：统一使用顶部进度条 + 节点脉冲动画
- **结算系统**：三星制（80%/50%/低于50%）+ 奖杯 + confetti + Yoyo 庆祝
- **音效体系**：Web Audio 合成（`useSfx.js` 7 种音效）+ MP3 音频双重保障
- **同局去重**：`usedWordIds` Set 保证 5 轮目标词不重复
- **词库来源**：`store.unlockedCategoryList` → `ALL_L1_WORDS` / `ALL_L2_WORDS`
- **语音系统**：`useSpeech.js` 优先预录 MP3，回退 TTS，不支持动态 `volume` 参数
- **离线支持**：所有游戏资源（预录 MP3、组件代码、词库）必须通过 PWA Service Worker 全量缓存，确保断网后游戏可正常启动（与 PRD 8.1 技术栈一致）

### 1.3 优点 ✅

- UI/UX 打磨精良：粒子动画、弹性反馈、正确反馈覆盖层 —— 视觉质量远超幼儿 App 平均水平
- 难度分级体系完整：三档动态调整选项数 + 网格布局，已接入 ParentView
- 音效反馈链路完善：正确/错误/翻牌/完成都有独立音效
- Yoyo 全程陪伴：6 种状态 (idle/thinking/happy/encourage/celebrate) 随游戏阶段切换
- 结算仪式感强：confetti + 奖杯 + 鼓励语 + 宝贝头像庆祝

---

## 二、核心问题诊断

### 2.1 同质化严重 🔴

MatchGame 与 ListenGame **本质是同一个玩法**，区别仅在于选项卡是否显示英文文字：
- MatchGame：只显示 emoji
- ListenGame：显示 emoji + 英文单词

对 3-8 岁孩子来说体验差异太小，家长和孩子感受不到"换了一款游戏"。

### 2.2 缺少时间压力与紧迫感 🔴

当前三款游戏都**没有时间限制**：
- 孩子可以慢慢看、慢慢选
- 翻翻乐也没有计时挑战
- 缺少"快！再快！"的刺激感

幼儿游戏的"再来一局"冲动很大程度来自时间压力带来的兴奋感。

### 2.3 连击 (Combo) 系统缺失 🔴

目前每轮答对固定 +1 ⭐：
- 连续答对 5 次 = 5 ⭐
- 答错重答 = 还是 +1 ⭐/轮

缺少**连击乘数**的爽快感，没有 Duolingo 式的 combo fire 激励。

### 2.4 词库利用率不均 🟡

120 个单词中，**Actions（动作）**和 **Emotions（情感）**两个分类（各10词）几乎没有在现有游戏中得到针对性利用。特别是"动作"类单词天然适合 TPR（全身反应法）游戏。

### 2.5 结算奖励缺乏递进感 🟡

- 三星阈值：80%/50%/低于50% —— 跨度太大
- 游戏得分只存最高分 `gameScores`，不记录累计成就
- 连续答对全部 5 轮 vs 答对 4 轮的奖励感差异不明显

### 2.6 缺少纯粹的"奖励型"游戏 🟡

当前系统只有"完成→撒花→结算"，缺少一个**主动参与的庆祝环节**。孩子从被动接受奖励变成主动"挣到"奖励，成就感完全不同。

---

## 三、现有游戏优化建议

### 3.1 P0 — 差异化 MatchGame 与 ListenGame

#### MatchGame 改造为「听音盲选」模式

| 维度 | 改造后设计 |
|------|-----------|
| **核心** | 听音后**只显示 emoji 卡片**，不显示文字 |
| **干扰词设计** | 从同类别中选相似 emoji（如 dog🐕 的干扰项用 cat🐈/pig🐷） |
| **认知目标** | 训练纯粹的"语音→语义"映射 |
| **适用阶段** | L1 启蒙（3-4岁）|

#### ListenGame 改造为「单词认读」模式

| 维度 | 改造后设计 |
|------|-----------|
| **核心** | L2 阶段听音后**只显示英文单词卡片**，不显示 emoji |
| **渐进策略** | L1 保留 emoji 辅助，L2 逐步过渡到纯文字 |
| **认知目标** | 训练"听音→拼写"映射 |
| **适用阶段** | L2 进阶（4-6岁）|

**改造后差异对比**：

| 维度 | MatchGame (盲选) | ListenGame (认读) |
|------|-----------------|------------------|
| 选项内容 | 纯 emoji | 纯英文文字（L2）|
| 认知维度 | 语音→语义 | 语音→拼写 |
| 适用年龄 | 3-4 岁 | 4-6 岁 |
| 干扰策略 | 同类别相似 emoji | 拼写相近单词 |

### 3.2 P0 — 增加 Combo 连击系统

#### 连击规则

```
连续答对 1 轮 → Combo x1 → ⭐ +1
连续答对 2 轮 → Combo x2 → ⭐ +2  
连续答对 3 轮 → Combo x3 → ⭐ +3（🔥 火焰特效）
连续答对 5 轮 → Combo x5 → ⭐ +5（🔥🔥🔥 全屏火焰）

答错后 Combo 归零，但当前轮仍给 ⭐ +1 鼓励
```

#### UI 表现

- 屏幕右上角显示 combo 计数器
- x3 以上时数字带火焰动效 + 脉冲光晕
- x5 时触发"Combo Fever" —— 背景短暂变金色（0.5 秒）
- **首次达到 Combo x3 时**：屏幕中央显示大型 🔥 火焰动画 + 呦呦配音"哇！连续答对3个！"（帮助 3 岁儿童理解连击概念）

#### 家长控制

> 家长中心增加 **"Combo 提示开关"**：默认开启，家长可关闭火焰特效和配音（避免过度兴奋或打扰）。

#### 数据追踪

```js
// stores/learning.js 新增字段
gameCombo: 0           // 当前连击数（不持久化，局内有效）
gameMaxCombo: 0        // 本局最大连击（不持久化）
lifetimeMaxCombo: 0    // 历史最大连击（持久化到 user_progress）

function addCombo() {
  gameCombo.value++
  if (gameCombo.value > gameMaxCombo.value) gameMaxCombo.value = gameCombo.value
  if (gameCombo.value > lifetimeMaxCombo.value) lifetimeMaxCombo.value = gameCombo.value
}
function resetCombo() { gameCombo.value = 0 }
function getComboBonus() { 
  return gameCombo.value >= 3 ? gameCombo.value : 1 
}
```

---

## 四、精选 3 款 MVP 新游戏

> **决策背景**：从 6 款候选新游戏中精选 3 款，优先解决"现有游戏类型覆盖不全"的核心问题。
> 选择标准：① 与现有游戏玩法差异度 ② 开发成本（复用代码比例）③ 解决的痛点优先级

### 精选结果

| 优先级 | 游戏 | 类型 | 解决的痛点 | 开发成本 |
|--------|------|------|-----------|---------|
| **🥇 #1** | **Catch Stars（接星星）** | 纯收集·奖励型 | 填补"学习→奖励"链路空白 | ⭐⭐ 低 |
| **🥈 #2** | **Speed Rush（速度大挑战）** | 限时·听力辨识 | 增加时间压力 + 复用 MatchGame 70% | ⭐⭐ 低 |
| **🥉 #3** | **Sort It!（快分类）** | 分类·认知推理 | 唯一覆盖分类认知维度 | ⭐⭐⭐ 中 |

**三款游戏的节奏定位**：紧张（Speed Rush）+ 放松（Catch Stars）+ 思考（Sort It!），完整覆盖幼儿游戏需要的"心跳-开心-动脑"三种体验。

---

### 4.1 Game 4：「接星星」Catch the Stars ⭐（奖励游戏，第 1 款新游戏）

| 维度 | 设计 |
|------|------|
| **触发条件** | 首次完成一个分类学习 / 游戏通关（3⭐）后自动弹出 |
| **冷却机制** | 同一分类学习后 **24 小时内不再触发**（防止奖励通胀和体验疲劳） |
| **时长** | 15 秒（足够开心，不会拖沓） |
| **掉落物** | ⭐（普通 70%）、🎁（大奖 25%）、💎（稀有 5%） |
| **核心玩法** | 点击掉落的星星/红包收集，限时 15 秒 |
| **互动方式** | 纯点击（iPad 上小手点屏幕，简单直接） |
| **结算** | 收集到的 ⭐ 通过 `store.addStars()` 加到 totalStars |
| **点击保护** | 200ms 冷却 + 每局最多收集 15 个 + 60px 最小点击区域 |
| **收集上限** | L1: 10 个 / L2: 15 个（根据学习阶段动态调整） |

#### 触发与冷却规则

```js
// stores/learning.js 新增
const catchStarsCooldown = ref({})  // { categoryId: timestamp }

function canTriggerCatchStars(categoryId) {
  const lastTrigger = catchStarsCooldown.value[categoryId]
  if (!lastTrigger) return true
  const hoursSince = (Date.now() - lastTrigger) / (1000 * 60 * 60)
  return hoursSince >= 24  // 24 小时冷却
}

function recordCatchStarsTrigger(categoryId) {
  catchStarsCooldown.value[categoryId] = Date.now()
}
```

```
触发场景：
✅ 首次完成一个分类全部 10 个单词学习
✅ 首次任意游戏通关（3⭐ 评价）
✅ 连续学习 3 天以上（稀有掉落率翻倍：💎 10%）

冷却规则：
❌ 同一分类 24 小时内不再触发（即使再次完成该分类学习）
❌ 普通答对一题（太频繁，失去奖励感）
❌ 复习环节（复习是常态，不需要每次奖励）
```

#### 游戏流程

```
1. 🐯 "你太棒了！星星掉下来啦！快接住它们！"
2. 屏幕顶部开始掉落 ⭐🎁💎（随机位置、随机速度、随机旋转）
3. 孩子点击收集：
   ⭐  → +1 星星（占 70%）
   🎁  → +3 星星（占 25%，打开动画 + 特殊音效）
   💎  → +5 星星（占 5%，金色闪光特效 + 稀有音效）
4. 15 秒倒计时结束 → 🐯 "太厉害了！收集了 X 个星星！"
5. 结算显示收集总数，关闭后回到上一页
```

#### 掉落物配置

```js
const DROP_TYPES = [
  { emoji: '⭐', weight: 70, value: 1, sound: 'star' },    // sfxStar()
  { emoji: '🎁', weight: 25, value: 3, sound: 'match' },   // sfxMatch()
  { emoji: '💎', weight: 5,  value: 5, sound: 'complete' } // sfxComplete()
]

const DROP_INTERVAL = 800       // 每 0.8 秒掉落一个
const MAX_ON_SCREEN = 8         // 同时屏幕上的掉落物上限
const GAME_DURATION = 15        // 游戏时长（秒）
const MAX_COLLECTION = { l1: 10, l2: 15 } // 按学习阶段动态上限
const CLICK_COOLDOWN = 200      // 点击冷却（ms，防止乱点）
```

#### CSS 掉落动画

```css
@keyframes dropFall {
  0% { transform: translateY(-60px) rotate(0deg); opacity: 1; }
  80% { opacity: 1; }
  100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
}

@keyframes collectPop {
  0% { transform: scale(1); }
  50% { transform: scale(1.8); opacity: 0.8; }
  100% { transform: scale(0); opacity: 0; }
}

.falling-item {
  position: absolute;
  top: -60px;
  animation: dropFall var(--duration) linear forwards;
  cursor: pointer;
  font-size: 3rem;
  user-select: none;
  width: 60px;
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.falling-item.collected {
  animation: collectPop 0.4s ease forwards;
  pointer-events: none;
}
```

#### 组件结构

```vue
src/components/rewards/CatchGame.vue
├── 掉落物生成器（setInterval 每 0.8 秒生成一个）
├── 点击收集处理（事件委托 + 200ms 冷却）
├── 倒计时显示（右上角圆形进度条，复用 sfxTick）
├── 收集计数器（左下角实时显示 "已收集: 8 ⭐"）
├── 关闭按钮（右上角 ×，可跳过）
└── 冷却状态检查（store.canTriggerCatchStars）
```

#### 技术可行性评估

| 维度 | 评估 |
|------|------|
| 代码复用 | `useConfetti.js` 掉落逻辑可直接复用，音效 3 种已有 |
| 新建文件 | `src/components/rewards/CatchGame.vue` + `src/data/drops.js`（配置） |
| 预计代码量 | ~350 行组件 + ~50 行配置 |
| 风险 | 无，独立组件不阻塞任何现有逻辑 |

---

### 4.2 Game 5：「速度大挑战」Speed Rush（限时游戏，第 2 款新游戏）

| 维度 | 设计 |
|------|------|
| **核心玩法** | 限时内尽可能多地匹配单词 |
| **游戏节奏** | Simple: 45s / Medium: 35s / Hard: 30s 倒计时 |
| **时间经济** | 答对 +3 秒，Combo x3 +5 秒，Combo x5 +8 秒 |
| **时间硬上限** | **单局最长不超过 90 秒**（防止时间奖励无限延长） |
| **认知维度** | 听力辨识（同 MatchGame，但加时间压力） |
| **难度** | 选项数固定 3 个（降低认知负荷，让速度成为唯一挑战） |
| **结算** | 按答对总数给 ⭐ + 时间奖励 ⭐（>20 题额外 +1⭐） |
| **趣味点** | 时间 <10 秒时呦呦做出"着急"表情 + 轻声说"加油！快一点！" |
| **代码改动量** | 中等（复用 MatchGame 70% 代码，增加 timer 逻辑） |

#### 核心循环

```
听音 → 3个emoji中选 → 答对(+3秒) → 下一个 → 时间到 → 结算

连击 bonus：
  Combo x3 → 额外 +5 秒 + 屏幕撒花
  Combo x5 → 额外 +8 秒 + 特殊音效

时间硬上限：
  无论如何，totalTime 不能超过 90 秒
  time = Math.min(time + bonus, 90)
```

#### 数据结构

```js
const SPEED_RUSH_CONFIG = {
  gameId: 'speed-rush',
  timeLimitByDifficulty: { simple: 45, medium: 35, hard: 30 },
  correctBonus: 3,        // 答对加秒
  comboBonus: 5,          // 连击加秒
  combo5Bonus: 8,         // 5连击额外加秒
  optionsCount: 3,        // 固定选项数
  wordPool: 'unlocked',   // 词库来源
  urgencyThreshold: 10,   // 时间 <10s 触发紧急状态
  maxTime: 90             // 单局硬上限（秒）
}
```

#### 儿童适配

> Simple 模式 45 秒给 3 岁孩子充裕时间。时间紧迫状态**不用红色闪烁 + 心跳音效**（对幼儿过于焦虑），改为呦呦表情变化 + 语音鼓励。时间达到硬上限 90 秒时，呦呦说"时间到啦！你太棒了！"而不是显示倒计时归零。

#### 技术可行性评估

| 维度 | 评估 |
|------|------|
| 代码复用 | MatchGame 核心逻辑（听音→3选1→去重）70% 可复用 |
| 新增文件 | `src/components/games/SpeedRushGame.vue` |
| 新增逻辑 | Timer 倒计时 + 时间加秒 + 硬上限 + 紧急状态 UI |
| 预计代码量 | ~450 行 |
| 风险 | 低，核心玩法已验证 |

---

### 4.3 Game 6：「快分类」Sort It!（分类游戏，第 3 款新游戏）

| 维度 | 设计 |
|------|------|
| **核心玩法** | 点击将单词卡片送到正确的分类篮子中 |
| **游戏节奏** | 10 个单词 |
| **限时** | Simple: 不计时 | Medium: 90 秒 | Hard: 75 秒 |
| **认知维度** | 分类理解 + 词汇辨识 |
| **难度** | 2 个篮子(Simple) → 3 个篮子(Medium/Hard) |
| **结算** | 按正确率给 ⭐（100% = 3⭐，80% = 2⭐，60% = 1⭐） |
| **趣味点** | 分类成功时篮子会"吃掉"单词 + 咀嚼动画 |
| **代码改动量** | 中等（新建组件） |

#### 核心循环

```
屏幕中间出现一个单词卡片（emoji + 文字）
下方 2-3 个分类篮子（🐾 / 🍎 / 🎨）
点击正确篮子 → 单词飞进篮子 + 咀嚼声 → +1分
点击错误篮子 → 篮子摇头 → 不扣分，再试

10 个单词分完 → 结算
```

#### 篮子配置 — 动态主题分组方案

```js
// 按学习阶段预设主题分组，减少认知冲突
const THEME_GROUPS = {
  l1: [
    { name: '自然组', categories: ['animal', 'fruit'] },      // 动物+水果
    { name: '感知组', categories: ['color', 'body'] },         // 颜色+身体
    { name: '数字组', categories: ['number'] },                // 单独数字
  ],
  l2: [
    { name: '生活组', categories: ['clothing', 'transport'] }, // 衣服+交通工具
    { name: '空间组', categories: ['places', 'home'] },        // 地点+家居
    { name: '情感组', categories: ['emotions', 'actions'] },   // 情感+动作
  ]
}

function generateBaskets(currentLevel, difficulty) {
  const basketCount = { simple: 2, medium: 3, hard: 3 }[difficulty]
  const unlockedCats = store.unlockedCategoryList
  
  // 优先使用主题分组
  const themeGroup = THEME_GROUPS[currentLevel]?.find(group => 
    group.categories.every(cat => unlockedCats.some(u => u.id === cat))
  )
  
  if (themeGroup) {
    return themeGroup.categories.slice(0, basketCount).map(catId => {
      const cat = unlockedCats.find(u => u.id === catId)
      return { id: cat.id, emoji: cat.emoji }
    })
  }
  
  // 回退：从已解锁分类中选取
  return unlockedCats.slice(0, basketCount).map(cat => ({
    id: cat.id,
    emoji: cat.emoji,
  }))
}
```

> Simple 模式默认使用 "动物+水果" 主题组（L1 最熟悉的两个分类），避免"数字 vs 颜色"这类抽象混合导致的认知冲突。

#### 词源规则

- 只从**篮子对应分类**的词库中选取单词
- 每个篮子的单词数量平均分配（10 个单词 / 2-3 个篮子）
- 确保每个篮子至少有 2 个单词可分

#### 家长控制

> 家长中心增加 **"分类筛选"选项**：允许家长指定要练习的分类组合（如"今天只练动物和水果"），Sort It! 会优先使用家长指定的分类生成篮子。

#### 技术可行性评估

| 维度 | 评估 |
|------|------|
| 新建文件 | `src/components/games/SortGame.vue` |
| 新增逻辑 | 主题分组篮子生成 + 飞入篮子动画 + 咀嚼音效 |
| 预计代码量 | ~450 行 |
| 风险 | 中等，飞入动画需要 CSS transform + transition 精准控制 |

---

## 五、后续迭代游戏（本期不实施）

> 以下 3 款游戏设计保留，但不作为 MVP 新游戏实施。等待后续迭代时再开发。

### 5.1 Treasure Hunt（寻宝猎人）

**延期原因**：线索系统 MVP 覆盖 30 词即可，但认知上与 MatchGame 重叠度高（都是听力选择）。分类推理维度已有 Sort It! 覆盖。

**前置条件**：线索模板系统完善到覆盖 60+ 词。

### 5.2 Whisper Challenge（悄悄话）

**延期原因**：字母碎片揭示方案对 3 岁孩子价值有限（还不会认字母），L1 阶段不适合。L2 阶段文字认读需求更强时再开发。

**前置条件**：L2 阶段文字认读需求验证后启动。

### 5.3 YoYo Story（呦呦剧场）

**延期原因**：`words.js` 无词性标注，选词组合会产生语法错误句子。

**前置条件**：`words.js` 增加 `pos`（词性）字段后再开发。

---

## 六、游戏系统架构优化

### 6.1 Store 扩展

```js
// stores/learning.js 新增字段
gameCombo: 0,              // 当前连击数（不持久化）
gameMaxCombo: 0,           // 本局最大连击（不持久化）
lifetimeMaxCombo: 0,       // 历史最大连击（持久化到 user_progress）
speedMode: false,          // 是否开启限时模式（不持久化）
catchStarsCooldown: {},    // { categoryId: timestamp } 冷却状态
showComboGuide: true,      // 是否显示 Combo 首次引导（家长控制）
dynamicCatchLimit: true,   // 是否启用动态收集上限（L1=10, L2=15）

// 新增方法
function addCombo() {
  gameCombo.value++
  if (gameCombo.value > gameMaxCombo.value) gameMaxCombo.value = gameCombo.value
  if (gameCombo.value > lifetimeMaxCombo.value) lifetimeMaxCombo.value = gameCombo.value
}
function resetCombo() { gameCombo.value = 0 }
function getComboBonus() { 
  return gameCombo.value >= 3 ? gameCombo.value : 1 
}
function canTriggerCatchStars(categoryId) {
  const last = catchStarsCooldown.value[categoryId]
  if (!last) return true
  return (Date.now() - last) >= 24 * 60 * 60 * 1000
}
function recordCatchStarsTrigger(categoryId) {
  catchStarsCooldown.value[categoryId] = Date.now()
}
function getCatchStarsLimit() {
  // 根据当前学习阶段动态返回上限
  return dynamicCatchLimit.value 
    ? (store.currentLevel === 'l2' ? 15 : 10) 
    : 15
}

// ⚠️ 不新增 addRewardStars()，直接使用现有 addStars() 方法（第 362 行）
// CatchGame 中直接调用 store.addStars(collectedStars)
```

#### 持久化适配

```js
// persistProgress() 中新增字段
function persistProgress() {
  dbPut('user_progress', JSON.parse(JSON.stringify({
    key: 'main',
    unlockedCategories: unlockedCategories.value,
    totalStars: totalStars.value,
    gameScores: gameScores.value,
    firstUseTime: firstUseTime.value,
    todayLearnedCount: todayLearnedCount.value,
    todayDate: todayDate.value,
    avatar: avatar.value,
    themeColor: themeColor.value,
    gameDifficulty: gameDifficulty.value,
    dailyActivity: dailyActivity.value,
    lifetimeMaxCombo: lifetimeMaxCombo.value,      // 新增
    catchStarsCooldown: catchStarsCooldown.value,   // 新增
    showComboGuide: showComboGuide.value,           // 新增
    dynamicCatchLimit: dynamicCatchLimit.value      // 新增
  })))
}
```

#### gameScores 扩展

```js
// stores/learning.js 第 111 行扩展
const gameScores = ref({
  match: 0, listen: 0, memory: 0, balloon: 0,  // 已有
  'speed-rush': 0,                              // Speed Rush 最高分
  'sort-it': 0,                                 // Sort It! 最高分
})
```

### 6.2 游戏选择矩阵（MVP 3 款新游戏）

| 游戏 | 听力辨识 | 文字认读 | 记忆配对 | 时间压力 | 分类推理 | 奖励型 |
|------|:--------:|:--------:|:--------:|:--------:|:--------:|:------:|
| MatchGame (盲选) | ✅ | | ✅ | | | |
| ListenGame (认读) | ✅ | ✅ | | | | |
| MemoryGame | | | ✅ | | | |
| **Catch Stars** | | | | | | ✅ |
| **Speed Rush** | ✅ | | ✅ | ✅ | | |
| **Sort It!** | | ✅ | | | ✅ | |

> 3 款新游戏上线后，6 个认知维度全部覆盖（原有缺失：时间压力、分类推理、奖励型）

### 6.3 游戏解锁规则（v1.3 更新）

```
首页游戏入口：
  └── 🎮 游戏工坊
       ├── Game 1 找一找（听音盲选）—— 默认解锁
       ├── Game 2 听音选词（单词认读）—— 默认解锁
       ├── Game 3 翻翻乐（记忆配对）—— 默认解锁
       ├── Game 4 速度大挑战 —— ⚠️ v1.3 调整：累计游戏 2 次后解锁（原 5 次）
       └── Game 5 快分类 —— ⚠️ v1.3 调整：完成任意 2 个分类学习后解锁（原"掌握20词"）

奖励游戏（自动触发）：
  ├── 首次完成一个分类学习 → 弹出 Catch Stars（24小时冷却）
  ├── 首次游戏通关（3⭐） → 弹出 Catch Stars（24小时冷却）
  └── 连续学习 3 天 → 弹出 Catch Stars（稀有掉落率翻倍：💎 10%）
```

> **解锁门槛调整说明**：
> - Speed Rush 从"累计游戏5次"降到"2次"：3岁幼儿耐心有限，降低门槛能更快体验限时玩法
> - Sort It! 从"掌握20个单词"改为"完成2个分类学习"：L1 总词量 50，要求掌握 40% 过难

### 6.4 离线模式支持

> 所有游戏资源必须通过 PWA Service Worker 全量缓存，确保断网后游戏可正常启动：
> - 预录 MP3 音频（`/sounds/` 目录）
> - 游戏组件代码（`src/components/games/*.vue`）
> - 词库数据（`src/data/words.js`）
> - 音效库（`src/composables/useSfx.js`）
> - 语音库（`src/composables/useSpeech.js`）
>
> 与 PRD 8.1 技术栈（PWA + IndexedDB）完全兼容。断网状态下，所有游戏仍可正常启动和游玩，仅 TTS 回退功能不可用（依赖浏览器在线语音引擎）。

---

## 七、实施路线图（聚焦 3 阶段）

### 7.1 优先级矩阵

| 优先级 | 任务 | 类型 | 预计工作量 | 效果 |
|--------|------|------|-----------|------|
| 🥇 P0 | MatchGame 改"盲选" + ListenGame 改"认读" | 优化 | 1-2 小时 | 解决同质化 |
| 🥇 P0 | Combo 连击系统（含首次引导 + 家长开关） | 优化 | 2-3 小时 | 大幅提升爽快感 |
| 🥈 P1 | Game 4 Catch Stars（含冷却 + 动态上限） | 新增 | 3-4 小时 | 填补奖励游戏空白 |
| 🥈 P1 | Game 5 Speed Rush（含硬上限 + 呦呦紧急状态） | 新增 | 3-4 小时 | 增加时间压力 |
| 🥈 P1 | Game 6 Sort It!（含主题分组 + 飞入动画） | 新增 | 4-5 小时 | 增加分类认知 |
| 🟡 跨阶段 | 家长中心游戏设置面板 | 优化 | 2-3 小时 | 独立游戏开关/难度/解锁 |

### 7.2 分阶段实施计划

#### Phase 1：现有游戏优化 + Combo（1 天 + 儿童测试）

- [ ] 修改 MatchGame 为"盲选"模式（移除选项文字）
- [ ] 修改 ListenGame 为"认读"模式（L2 隐藏 emoji）
- [ ] 实现 Combo 连击系统（store 新增字段 + UI 组件）
- [ ] 首次 Combo x3 视觉引导（大型 🔥 动画 + 呦呦配音）
- [ ] 更新 3 款现有游戏接入 combo（MatchGame / ListenGame / MemoryGame）
- [ ] `lifetimeMaxCombo` 持久化到 `persistProgress()`
- [ ] `gameScores` 扩展新增 key
- [ ] **🧪 儿童测试**：完成后找 2-3 个 3-5 岁孩子测试，验证 Combo 视觉反馈是否被理解

#### Phase 2：Catch Stars 奖励游戏（1 天）

- [ ] 创建 `CatchGame.vue` 组件
- [ ] 实现掉落物动画 + 点击收集（200ms 冷却 + 动态上限 L1=10/L2=15）
- [ ] 实现 24 小时冷却机制（`catchStarsCooldown` store）
- [ ] 集成到完成流程（首次完成学习/游戏后弹出，检测冷却状态）
- [ ] 调用 `store.addStars()` 存储收集到的星星
- [ ] 倒计时 + 结算 UI
- [ ] **🧪 触发时机测试**：完成分类学习后弹出时机是否合适

#### Phase 3：2 款新游戏 + 家长设置（2 天）

- [ ] **Day 1**：Game 5 Speed Rush（复用 MatchGame，增加 timer + 90秒硬上限）
- [ ] **Day 2**：Game 6 Sort It!（主题分组篮子 + 飞入动画 + 家长分类筛选）
- [ ] 首页游戏入口更新（新解锁规则：Speed Rush 2次 / Sort It! 2个分类）
- [ ] **家长中心游戏设置面板**：每款游戏独立开关/难度/解锁条件
- [ ] 全链路测试与调优

### 7.3 成功指标

| 指标 | 目标 | 测量方式 |
|------|------|---------|
| 游戏完成率 | ≥ 85% | store.gameScores 记录 |
| 平均连击数 | ≥ 2.5 | lifetimeMaxCombo 统计 |
| 奖励游戏参与度 | ≥ 90% | 完成学习/游戏后 Catch Stars 弹出率 |
| "再来一局"意愿 | ≥ 3 次/局 | 游戏重玩次数统计 |
| 单次游戏时长 | 3-5 分钟 | session 计时 |
| 儿童理解度 | Combo x3 引导首次理解率 ≥ 80% | Phase 1 儿童测试 |

### 7.4 V1.1 迭代规划（MVP 之后）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 家长报告-游戏表现 | 学习报告中增加"游戏表现"板块：最高连击数、各游戏最高分、平均答对率 | P1 |
| Speed Rush "今日最佳" | 结算界面显示"今天你最快的一次！"记录 | P1 |
| Catch Stars "复习卡"掉落 | 掉落物新增"复习卡"，使用后强制复习一个单词 | P2 |
| Treasure Hunt | 线索模板系统完善后开发 | P2 |
| Whisper Challenge | L2 文字认读需求验证后开发 | P2 |
| YoYo Story | `words.js` 增加 `pos` 字段后开发 | P2 |

---

## 附录 A：游戏设计原则

### 幼儿游戏设计 10 条铁律

1. **永远不惩罚**：答错不扣分、不显示 ❌，只说"再试试呀~"
2. **即时反馈**：点击后 100ms 内必须有视觉/听觉响应
3. **短时高频**：每局 2-5 分钟，让孩子"再来一局"而不是"好累"
4. **简单规则**：一句话能讲清楚玩法（"听声音，找图片！"）
5. **可控难度**：孩子能通过练习明显感受到"我变强了"
6. **随机奖励**：不定比率奖励（variable ratio）最让人上瘾
7. **身体参与**：能点、能拖、能拍屏幕的比只能看的有趣 10 倍
8. **IP 陪伴**：呦呦不是装饰，是"和你一起玩的朋友"
9. **去字幕化**：L1 阶段尽量不显示英文文字，建立语音→语义直达
10. **庆祝仪式**：完成后的庆祝要让家长在旁边看了都想鼓掌

---

## 附录 B：技术债务清单

| 债务 | 说明 | 优先级 |
|------|------|--------|
| 代码复用 | MatchGame/ListenGame 有大量重复代码，应提取 BaseGame 组件 | P2 |
| 难度配置 | 三档难度配置分散在各组件，应统一到 store 或配置表 | P2 |
| 结算逻辑 | 三星判定逻辑各组件各自实现，应提取公共方法 | P2 |
| 音效管理 | MP3 路径硬编码在组件中，应统一管理 | P3 |
| 词库过滤 | 各游戏各自过滤词库，应提供统一 API | P2 |
| **词性标注** | `words.js` 无 `pos` 字段，YoYo Story 需要前置 | P2（后续迭代前完成）|
| **语音音量** | `useSpeech.js` 不支持动态 volume，Whisper 需替代方案 | P2（后续迭代前完成）|

---

## 附录 C：竞品游戏借鉴来源

| 本产品游戏 | 借鉴来源 | 借鉴点 |
|-----------|---------|--------|
| MatchGame | Lingumi | 听音选图，不打断纠音 |
| ListenGame | 叽里呱啦 | 单词认读，渐进引入文字 |
| MemoryGame | 斑马AI课 | 翻牌配对，记忆训练 |
| Speed Rush | Duolingo | 限时挑战，combo 连击 |
| Sort It! | Khan Academy Kids | 分类认知，拖拽互动 |
| Catch Stars | Duolingo | 掉落收集，奖励释放 |

---

## 附录 D：审查问题修正清单（v1.0 → v1.1 → v1.2 → v1.3）

### v1.0 → v1.1 审查修正

| # | 缺陷 | 严重程度 | 修正状态 | 修正方案 |
|---|------|---------|---------|---------|
| 1 | `addRewardStars()` 与现有 `addStars()` 冲突 | P0 | ✅ 已修正 | 直接使用现有 `store.addStars()`，删除新方法 |
| 2 | Whisper Challenge 音量控制不可行 | P0 | ✅ 已修正 | 改用**字母碎片揭示**方案，推迟到后续迭代 |
| 3 | YoYo Story 无词性标注导致语法错误 | P0 | ✅ 已修正 | **推迟到后续迭代**，标记 `pos` 字段为前置条件 |
| 4 | Treasure Hunt 线索数据量 240 条不可持续 | P0 | ✅ 已修正 | 改用**模板化生成 + 分类预设**，推迟到后续迭代 |
| 5 | Sort It! 篮子硬编码与解锁不一致 | P1 | ✅ 已修正 | 从 `unlockedCategoryList` 动态生成篮子 |
| 6 | Speed Rush 红色闪烁对幼儿焦虑 | P1 | ✅ 已修正 | 改为呦呦"着急"表情 + 语音鼓励 |
| 7 | Catch Stars 无点击冷却可无脑乱点 | P1 | ✅ 已修正 | 增加 200ms 冷却 + 每局最大 15 个 + 60px 点击区域 |
| 8 | Speed Rush 30 秒对 3 岁太短 | P1 | ✅ 已修正 | Simple: 45s / Medium: 35s / Hard: 30s |
| 9 | Treasure Hunt Hard 模式 4×4=16 格太多 | P1 | ✅ 已修正 | 推迟到后续迭代时采用 3×4=12 格 |
| 10 | Catch Stars 飞入首页动画跨组件复杂 | P1 | ✅ 已修正 | MVP 先做简单结算弹窗，飞入动画后续迭代 |

### v1.2 → v1.3 二次评审意见整合

| # | 意见 | 严重程度 | 采纳状态 | 处理方案 |
|---|------|---------|---------|---------|
| 1 | Catch Stars 触发过于频繁 | P0 | ✅ 已采纳 | 首次触发 + 24 小时冷却机制 |
| 2 | Speed Rush 解锁门槛过高（5次→2次） | P0 | ✅ 已采纳 | 累计游戏 2 次后解锁 |
| 3 | Sort It! 解锁条件过难（掌握20词→完成2分类） | P0 | ✅ 已采纳 | 完成任意 2 个分类学习后解锁 |
| 4 | Speed Rush 时间经济无上限 | P0 | ✅ 已采纳 | 增加 90 秒硬上限 |
| 5 | Combo 首次 x3 需视觉引导 + 家长开关 | P1 | ✅ 已采纳 | 大型 🔥 动画 + 呦呦配音 + 家长中心开关 |
| 6 | Sort It! 篮子需主题场景分组 | P1 | ✅ 已采纳 | 预设"动物+水果"等主题组 |
| 7 | Catch Stars 动态收集上限（L1=10/L2=15） | P1 | ✅ 已采纳 | 根据学习阶段动态调整 |
| 8 | 补充离线模式说明 | P1 | ✅ 已采纳 | 新增 6.4 节，明确 SW 缓存要求 |
| 9 | 游戏得分与家长报告联动 | P2 | ⏳ V1.1 | 迭代规划中 |
| 10 | Speed Rush "今日最佳"记录 | P2 | ⏳ V1.1 | 迭代规划中 |
| 11 | Catch Stars 掉落"复习卡" | P2 | ⏳ V1.1 | 迭代规划中 |

---

> **文档状态**：✅ v1.3 — 二次评审意见全部整合（4 个 P0 + 4 个 P1 已采纳，3 个 P2 规划到 V1.1） → 可启动 Phase 1 实施
